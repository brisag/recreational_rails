<h1>All Trails</h1>

<section id="trails">
  <% @trails.each do |trail| %>
  <h2><%= trail.name %></h2>
  <p>Elevation Gain (feet): <%= trail.elevation %></p>
  <p>Dogs Allowed? <%= trail.dogs_allowed %></p>
  <% end %>
</section>
